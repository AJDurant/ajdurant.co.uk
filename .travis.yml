dist: xenial

# Install the apt prerequisites
addons:
  apt:
    packages:
      - python-pygments
      - hugo
    update: true

# Clean and don't fail
install:
  - rm -rf public || exit 0

# Build the website
script:
  - hugo -v
